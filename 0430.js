function solution(n) {
    var answer = [];
    for (let i = 0; i < String(n).length; i++) {
        answer = answer + String(n)[i]
    }
    return answer.reverse();
}


function solution(n) {
    var answer = [];
    for (let i = 0; i < String(n).length; i++) {
        let arr = answer.push(Strung(n)[i])
    }
    return arr.reverse();
}